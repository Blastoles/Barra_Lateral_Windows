{
  "name": "barra-lateral-windows",
  "version": "1.0.0",
  "description": "Sidebar Flutuante (Drawer) para Windows com Rust e Tauri v2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "format": "prettier --write .",
    "lint": "eslint src/**/*.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  }
}
